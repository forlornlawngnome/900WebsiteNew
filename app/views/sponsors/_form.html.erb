<%= form_for(@sponsor) do |f| %>
  <% if @sponsor.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@sponsor.errors.count, "error") %> prohibited this sponsor from being saved:</h2>

      <ul>
      <% @sponsor.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :logo_link %><br>
    <%= f.file_field :logo_link %>
	<% if f.object.logo_link? %>
	      <%= image_tag f.object.logo_link.url %>
	      <%= f.label :remove_logo_link %>
	      <%= f.check_box :remove_logo_link %> 
    <% end %>
  </div>
  <div class="field">
    <%= f.label :sponsor_url %><br>
    <%= f.text_area :sponsor_url %>
  </div>
  <div class="field">
    <%= f.label :active %><br>
    <%= f.check_box :active %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
