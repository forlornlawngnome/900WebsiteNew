<%= form_for(@categories_post) do |f| %>
  <% if @categories_post.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@categories_post.errors.count, "error") %> prohibited this categories_post from being saved:</h2>

      <ul>
      <% @categories_post.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :post_id %><br>
    <%= f.text_field :post_id %>
  </div>
  <div class="field">
    <%= f.label :category_id %><br>
    <%= f.text_field :category_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
