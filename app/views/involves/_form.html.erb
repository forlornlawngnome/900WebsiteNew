<%= form_for(@involf) do |f| %>
  <% if @involf.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@involf.errors.count, "error") %> prohibited this involf from being saved:</h2>

      <ul>
      <% @involf.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :blurb %><br>
    <%= f.text_area :blurb %>
  </div>
  <div class="field">
    <%= f.label :order %><br>
    <%= f.number_field :order %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
