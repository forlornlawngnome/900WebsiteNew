<%= form_for(@document_link) do |f| %>
  <% if @document_link.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@document_link.errors.count, "error") %> prohibited this document_link from being saved:</h2>

      <ul>
      <% @document_link.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :document_id %><br>
	<%= f.collection_select(:document_id, Document.all, :id, :name, {:prompt=>true}, {:class=>'my-custom-class'}) %>
  </div>
  <div class="field">
    <%= f.label :doc_link %><br>
    <%= f.text_area :doc_link %>
  </div>
  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
